<% if @job_seeker.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@job_seeker.errors.count, "error") %>.
    </div>
    <ul>
      <% @job_seeker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>