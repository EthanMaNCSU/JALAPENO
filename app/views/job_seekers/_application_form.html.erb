<%= form_for(@application) do |f| %>
  <%= render 'applications/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new application..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>